import * as React from "react"

const TestPage = () => (
    <div style={{ padding: '2rem', textAlign: 'center' }}>
        <h1>Next.js is working!</h1>
        <p>If you can see this page, Next.js is properly configured.</p>
        <a href="/">Go back to home</a>
    </div>
)

export default TestPage
